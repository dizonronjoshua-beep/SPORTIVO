/* =========================================================
   SPORTIVO — PUBLIC / Terms
   Page-specific JavaScript only.
   Shared state/auth helpers remain in app.js.
========================================================= */

document.addEventListener('DOMContentLoaded', () => {
  document.documentElement.dataset.sportivoPage = 'public-terms';
});
